# Настройка онбординг экрана

## ✅ Что добавлено:

### 🎬 **OnboardingScreen**
- **Автоматический показ** при каждом запуске приложения
- **Длительность**: 3 секунды
- **Анимация капибары** с движущимися частями тела
- **Плавное появление** с fade и scale эффектами

### 🎨 **Анимации капибары:**
- **Тело капибары**: Плавное движение влево-вправо
- **Ноги**: Вращение для имитации ходьбы
- **Загрузочная линия**: Бесконечное движение слева направо
- **Цвета**: Коричневые оттенки в египетском стиле

### 📱 **Навигация:**
- **Первый экран**: OnboardingScreen
- **Автоматический переход**: MainMenu через 3 секунды
- **Отключены жесты**: Пользователь не может пропустить онбординг

## 🔧 **Как это работает:**

1. **Приложение запускается** → показывается OnboardingScreen
2. **Анимация капибары** запускается автоматически
3. **Через 3 секунды** происходит переход на MainMenu
4. **Пользователь попадает** в главное меню игры

## 🎮 **Особенности анимации:**

### **Тело капибары:**
- Плавное движение влево-вправо каждую секунду
- Анимация синхронизирована для всех частей тела

### **Ноги:**
- Первая нога вращается в одну сторону
- Вторая нога вращается в противоположную сторону
- Создается эффект ходьбы

### **Загрузочная линия:**
- Пунктирная линия движется слева направо
- Полный цикл за 10 секунд
- Бесконечное повторение

## 📱 **Техническая реализация:**

### **React Native Animated API:**
- `fadeAnim`: Прозрачность для появления
- `scaleAnim`: Масштаб для появления
- `bodyAnim`: Движение тела капибары
- `legAnim`: Вращение первой ноги
- `leg2Anim`: Вращение второй ноги
- `loaderAnim`: Движение загрузочной линии

### **Автоматический переход:**
```typescript
useEffect(() => {
  const timer = setTimeout(() => {
    navigation.navigate('MainMenu');
  }, 3000);
  
  return () => clearTimeout(timer);
}, []);
```

## 🚀 **Запуск приложения:**

```bash
# Установка зависимостей
npm install

# Запуск Metro сервера
npm start

# Запуск на Android
npm run android

# Запуск на iOS
npm run ios
```

## 📋 **Проверка онбординга:**

1. **Запустите приложение**
2. **Должен показаться** экран с анимированной капибарой
3. **Через 3 секунды** автоматически перейдет на главное меню
4. **Капибара должна двигаться** и анимироваться

## 🔍 **Отладка:**

Если онбординг не работает:

1. **Проверьте консоль** на ошибки
2. **Убедитесь**, что OnboardingScreen добавлен в навигацию
3. **Проверьте**, что initialRouteName = "Onboarding"
4. **Перезапустите** Metro сервер

## 🎨 **Кастомизация:**

### **Изменить длительность:**
```typescript
const timer = setTimeout(() => {
  navigation.navigate('MainMenu');
}, 5000); // 5 секунд вместо 3
```

### **Изменить цвета:**
```typescript
backgroundColor: '#CC7D2D', // Основной цвет капибары
backgroundColor: '#533C1C', // Темный цвет деталей
```

### **Изменить размер:**
```typescript
capybaraloader: {
  width: 200,  // Больше размер
  height: 150,
}
```

## 📱 **Поддерживаемые платформы:**

- ✅ **Android**: Полная поддержка всех анимаций
- ✅ **iOS**: Полная поддержка всех анимаций

Теперь ваше приложение имеет красивый онбординг экран с анимированной капибарой! 🎉🐺✨
